{
  "rules": {
    "match": {
      ".read": true,
      ".write": "auth != null && (auth.token.firebase.sign_in_provider == 'password' || auth.token.firebase.sign_in_provider == 'google.com')"
    },
    "history": {
      ".read": true,
      ".write": "auth != null && (auth.token.firebase.sign_in_provider == 'password' || auth.token.firebase.sign_in_provider == 'google.com')"
    },
    "notifications": {
      ".read": true,
      ".write": "auth != null && (auth.token.firebase.sign_in_provider == 'password' || auth.token.firebase.sign_in_provider == 'google.com')"
    },
    "fcmTokens": {
      ".read": false,
      "$token": {
        ".write": "auth != null",
        ".validate": "newData.isBoolean()"
      }
    }
  }
}
